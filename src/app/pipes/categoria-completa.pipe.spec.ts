import { CategoriaCompletaPipe } from './categoria-completa.pipe';

describe('CategoriaCompletaPipe', () => {
  it('create an instance', () => {
    const pipe = new CategoriaCompletaPipe();
    expect(pipe).toBeTruthy();
  });
});
